"""
SESSION 03 — MULTI-FILE CONTEXT DEMO  (File to CREATE live)
============================================================
INSTRUCTOR: With add.py and subtract.py OPEN in editor tabs,
type the line below and watch Copilot complete the entire file
by reading the pattern from the two open sibling files.

Start typing:
    def multiply(

Then press Tab to accept the full suggestion.
Copilot should generate:
  - Same Number type alias
  - Same logger setup
  - Same docstring format (Args / Returns / Examples)
  - Same logging calls (debug + info)
  - A multiply_many() function matching subtract_many / add_many style
"""

# ← Start typing here. Keep add.py and subtract.py open in tabs.
